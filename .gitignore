# Material em bruto (fotos originais do Facebook) — não publicado
_source/

# Sistema
.DS_Store
Thumbs.db
*.log

# Ferramentas locais de desenvolvimento — o Pages serve a arvore toda
# (ha .nojekyll), por isso tudo o que ficar versionado fica publico.
.claude/
